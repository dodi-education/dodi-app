/**
 * @dodi/protocol — the cross-client API contract: typed transport client,
 * centralized request schemas, and the E2EE message envelope. Shared by web,
 * platform (validation), and the agent.
 */
export * from "./envelope";
export * from "./schemas";
export * from "./client";
